schema_version: '0.1'
slug: evm-token-due-diligence
name: "Assay \U0001F52C"
version: 0.1.4
summary: "Point it at one contract on one chain. It comes back with what privileged\
  \ actors can change or take, who can pull liquidity principal, whether you can actually\
  \ sell at your size, how concentrated ownership really is, and where the fees go\
  \ \u2014 every claim tied to a block number, a block hash and a raw artifact you\
  \ can re-run yourself. Unknowns stay unknown: no check that did not complete is\
  \ ever presented as a pass, and no verdict is ever an unconditional \"safe\". Read-only\
  \ by construction \u2014 no keys, no signing, no broadcast, enforced in code and\
  \ proved by the test suite."
primary_kind: script
tags:
- research
- security
- due-diligence
- evm
- tokens
- read-only
- liquidity
- forensics
components:
- id: main
  kind: script
  path: scripts/main.py
applet:
  build_dir: applet/dist
  manifest: applet/applet.manifest.json
skill:
  enabled: true
  source: generated
  name: evm-token-due-diligence
  description: 'Evidence-bounded due diligence on one exact EVM token and the economic
    system around it: privileged control and upgrade paths, liquidity custody across
    Uniswap v2/v3/v4, real sellability at holder size, supply concentration, launch
    integrity, fee and treasury flow, backing and redemption rights. Pins every claim
    to a block, separates proven from inferred from unknown, never marks an unknown
    as a pass, and never signs or broadcasts anything.'
  instructions: skill/instructions.md
  runtime:
    mode: thin
    package: wayfinder-paths
    version: 0.11.0
    python: '>=3.12,<3.13'
    component: main
    bootstrap: uv
    fallback_bootstrap: pipx
    prefer_existing_runtime: true
    require_api_key: false
    api_key_env: WAYFINDER_API_KEY
    config_path_env: WAYFINDER_CONFIG_PATH
